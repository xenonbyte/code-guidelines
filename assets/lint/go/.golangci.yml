version: "2"

linters:
  default: standard
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gocritic
    - revive
  exclusions:
    paths:
      - third_party$
      - vendor$

formatters:
  enable:
    - gofmt
    - goimports
