# Rendered by src/build/platforms.mjs for the Gemini CLI TOML command format
# (SPEC-PLATFORM-001, SPEC-BUILD-001). `description` is the universal negative-invocation-guard
# sentence (fragments.own['triggers.md'] trimmed) — same value as every other platform. `prompt`
# is a TOML literal multi-line string (no escaping needed) built from SHARED_BODY / PURPOSE /
# TRIGGERS / BEHAVIOR / OUTPUT, each fragments.shared['body.md'] / fragments.own['purpose.md' |
# 'triggers.md' | 'behavior.md' | 'output.md'] verbatim. These commands take no arguments, so
# there is no argument placeholder.
description = '{{DESCRIPTION}}'
prompt = '''
{{SHARED_BODY}}

## Purpose

{{PURPOSE}}

## Triggers

{{TRIGGERS}}

## Behavior

{{BEHAVIOR}}

## Output

{{OUTPUT}}
'''
